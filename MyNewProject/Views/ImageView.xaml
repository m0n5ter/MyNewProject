<UserControl x:Class="MyNewProject.Views.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyNewProject.Views"
             xmlns:viewModels="clr-namespace:MyNewProject.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
             >
    <Grid>
        <TextBox Text="{Binding Image.ImageUrl, UpdateSourceTrigger=PropertyChanged}"></TextBox>
    </Grid>
</UserControl>
