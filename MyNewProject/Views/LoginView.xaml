<UserControl 
    x:Class="MyNewProject.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModels="clr-namespace:MyNewProject.ViewModels"
    mc:Ignorable="d" 
    d:DesignWidth="200"
    d:DataContext="{d:DesignInstance viewModels:LOginViewModel}"
>
    <StackPanel>
        <TextBlock>User name</TextBlock>
        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Margin="0,5,0,0">Password</TextBlock>
        <TextBox Text="{Binding Password , UpdateSourceTrigger=PropertyChanged}"/>
        
        <Button Margin="0,5,0,0" Command="{Binding LoginCommand}">Login</Button>
        
        <TextBlock Text="{Binding HelloText}" Margin="0,5,0,0"/>

        <TextBlock Margin="0,5,0,0">Is logging in: <Run Text="{Binding IsLoggingIn}" FontWeight="Bold"/></TextBlock>

        <TextBlock Visibility="{Binding IsLoggingIn, Converter={StaticResource BooleanToVisibilityHiddenConverter}}" Text="WORKING....." FontSize="24"/>

        <TextBlock Margin="0,5,0,0">Is logged in: <Run Text="{Binding LoggedIn}" FontWeight="Bold"/></TextBlock>
    </StackPanel>
</UserControl>