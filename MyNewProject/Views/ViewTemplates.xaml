<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:views="clr-namespace:MyNewProject.Views"
                    xmlns:viewModels="clr-namespace:MyNewProject.ViewModels">

    <!--
        The routing table of the application: data type -> view.

        This is the only file where "the login view model" and "LoginView" are ever
        mentioned together. Nobody else knows that a view for a view model exists -
        a ContentControl is simply given an object and finds the template by its
        runtime type, exactly like the shapes on the canvas in lesson 7.

        Not a single x:Key and not a single DataContext below: the key would turn the
        implicit lookup off, and the DataContext is set by whoever applies the template.
    -->

    <DataTemplate DataType="{x:Type viewModels:LoginViewModel}">
        <views:LoginView Width="240"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:WorkspaceViewModel}">
        <views:WorkspaceView/>
    </DataTemplate>

    <!-- Used as tab content: WorkspaceView sets no ContentTemplate, so the
         TabControl falls back to these implicit templates. -->
    <DataTemplate DataType="{x:Type viewModels:TodoListViewModel}">
        <views:TodoListView Margin="20"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:ImageViewModel}">
        <views:ImageView Margin="20"/>
    </DataTemplate>

</ResourceDictionary>
